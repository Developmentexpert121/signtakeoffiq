In pipeline.ts, remove the 60-second per-sheet timeout wrapper in the Step 3 sheet loop.
Find this pattern in the Step 3 loop:
tsconst STEP3_SHEET_TIMEOUT_MS = 60_000;
const timedOut = await Promise.race([
  (async (): Promise<false> => {
    // ... all the rasterization + vision logic ...
    return false;
  })(),
  new Promise<true>((resolve) =>
    setTimeout(() => resolve(true), STEP3_SHEET_TIMEOUT_MS),
  ),
]);

if (timedOut) {
  logger.warn(`[pipeline] Step 3: sheet ${sheet.sheetId} timed out after ${STEP3_SHEET_TIMEOUT_MS / 1000}s — skipping`);
  step6SheetResults.push({ sheetId: sheet.sheetId, status: "timeout" });
  if (++step6ProcessedCount % STEP6_FLUSH_INTERVAL === 0) await flushStep6Progress(step6SheetResults);
}
Replace the entire Promise.race construct with just the inner async function body executed directly — no timeout wrapper, no timedOut variable, no timeout log. Keep all the rasterization and vision logic inside intact, just unwrap it from the race.
Do not touch the timeouts on Steps 4, 7, 8, or 9. After saving, rescan British Airways Lounge with Force Fresh AI Scan enabled.
Sonnet 4.6Claude is AI and can make mistakes. Please double-check responses.ShareContent/**
 * Sign Takeoff IQ — 10-Step Processing Pipeline
 *
 * Orchestrates the full PDF processing flow:
 * 1.  Intake job files from object storage
 * 2.  Parse drawing index (filter relevant sheets)
 * 3.  Rasterize floor-plan pages to PNG → upload to object storage
 * 4.  Extract plaque schedule (Clpasted<workflow>
<workflow_name>artifacts/api-server: API Server</workflow_name>
<status>RUNNING</status><run_id>8Eqd4VVnCdKIumpxTrNTA</run_id><timestamp>2026-04-22T00:59:16.294159+00:00</timestamp>
<logs>> @workspace/api-server@0.0.0 dev /home/runner/workspace/artifacts/api-server
> export NODE_ENV=develpasted(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.nonce='vEXclhHrEAR9gSdAlmHVOg==';d.innerHTML="window.__CF$cv$params={r:'9ef777106e17905c',t:'MTc3NjcyMjg4MA=='};var a=document.createElement('script');a.nonce='vEXclhHrEAR9gSdAlmHVOg==';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();