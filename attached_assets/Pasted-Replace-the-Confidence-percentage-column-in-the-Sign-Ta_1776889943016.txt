Replace the Confidence percentage column in the Sign Table with a clearer Source/Status badge that estimators can understand at a glance.
Find the Sign Table component in artifacts/web/src/pages/job-detail.tsx or similar. Find where the Confidence column is rendered (look for the confidence field and percentage display).
Replace the Confidence column header and cell rendering:
Change column header from "Confidence" to "Status".
Replace the confidence percentage display with a status badge based on the sign's source and confidence value:
tsxfunction SignStatusBadge({ source, confidence }: { source: string; confidence: number }) {
  if (source === "schedule") {
    return (
      <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800 border border-green-200">
        ✓ Verified
      </span>
    );
  }
  if (source === "training_correction") {
    return (
      <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800 border border-blue-200">
        🎓 Trained
      </span>
    );
  }
  if (source === "rules_engine" && confidence >= 0.85) {
    return (
      <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800 border border-yellow-200">
        ⚡ Rules
      </span>
    );
  }
  // AI vision — needs review
  return (
    <span className="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium bg-purple-100 text-purple-800 border border-purple-200">
      🤖 AI — Review
    </span>
  );
}
Use this component in the Sign Table row where confidence is currently displayed.
Also update the Sign Table header row — add a small legend below the table header:
🤖 AI — Review  ⚡ Rules  🎓 Trained  ✓ Verified
Keep the existing confidence percentage accessible as a tooltip on hover:
tsxtitle={`${Math.round(confidence * 100)}% confidence`}
So estimators can still see the raw number if they need it, but the default view shows a clear actionable status.
After saving, share a screenshot of the Sign Table — it should now show colored badges instead of percentages, making it immediately clear which signs need human review vs which are already verified.
Sonnet 4.6